/**
 * Examples:
 * archie get # get the entire config
 * archie get archivers # list archivers
 * archie get archivers.twitter # get twitter archiver config
 * archie get archivers.twitter.uri # get twitter archiver config uri
 * archie get dataDir # get data directory
 * archie get archivers.twitter.uri dataDir # get twitter archiver config uri and data directory and output to new line
 */

export default function get() {
  // TODO: const args = useArgs();
}